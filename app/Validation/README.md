# Validation

Custom validation rule groups go here when CI4's built-in rules aren't enough
(e.g. cross-field checks). Reference each group from a Model's `$validationRules`
as documented in that model's matching "Prompt" block in Section 8 of the
architecture doc. Empty for now -- most rules in this project are simple enough
to live directly on the Model.
